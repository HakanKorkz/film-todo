import styles from './index.module.css'

function Header() {
    return (
        <header className={styles.header}>
           <div>1</div>
            <div>2</div>
        </header>
    );
}

export default Header;
